- 청크 업로드를 진행할 때 lock을 upload_chunk의 unique constraint로 구현
- 파일을 먼저 쓰고 upload_chunk insert로 판단할지, insert를 먼저 해서 이미 존재하면 분기할지는 고민이 필요
  - 전자의 경우 임시 파일로 저장해야 함.
- 청크 업로드의 중복 파일 처리도 고민해야 함. merge 이후에 해시가 나올텐데 이때 중복되면 어떻게 해야하는지.

### 2026-08-03 (월)
- 썸네일에 대한 정보를 저장할지 고민.
- 업로드할 때 파일에 대한 정보를 먼저 DB에 저장하고 응답할지 고민. ffmpeg + ffprobe는 이후에 하고 클라이언트에서 polling.
- 업로드 엔드포인트를 /api/media/uploads에서 /api/uploads로 독립시킬지 고민. 어차피 이제 /api/media에 POST요청으로 업로드하지 않으니까.


### 2026-08-04 (화)
- single-upload에서 해시 검사를 파일을 쓰고 난 이후로 변경하는 것을 고려하고 있음 (chunk-upload와 통일)
  - oriHash를 받고, 있으면 파일 쓰기 전 세션 삭제 -> 파일까지 받고 해시 검사 & 중복이면 삭제
- 에러 처리, cleanup에 대해 깊게 고민할 필요 있음
  - 에러가 발생하면 관련된 파일을 삭제? status만 변경?
- 후처리 작업을 큐로 만들 필요가 있음
  - BullMQ라는 도구가 있다는데 여기까지 가는 건 과하고 적어도 큐를 만들어서 관리해야 함. 적어도 동시성 제어라도.
- status를 추가하거나 세분화하기
